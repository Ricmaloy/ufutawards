import { Loading as LoadingIcon } from '@/images/Loading'

export const Loading = () => {
  return (
    <div className="flex h-screen w-full items-center justify-center">
      <LoadingIcon />
    </div>
  )
}
